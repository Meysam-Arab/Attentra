<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_user_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="ir.fardan7eghlim.attentra.views.track.TrackIndexActivity"
    tools:showIn="@layout/activity_track_index"
    android:orientation="vertical">
    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/search_box_layout_ti"
        android:layoutDirection="ltr"
        android:visibility="gone"
        android:descendantFocusability="beforeDescendants"
        android:focusableInTouchMode="true"
        android:layout_marginLeft="30dp"
        android:layout_marginBottom="5dp"
        android:layout_marginRight="30dp">


        <EditText
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:inputType="textPersonName"
            android:hint="چیزی بنویسید...."
            android:paddingLeft="15dp"
            android:textColorHint="#a5a09c"
            android:background="@drawable/edittext_rounded_search_box"
            android:ems="10"
            android:textColor="@color/colorPrimary"
            android:id="@+id/search_et_ti"
            android:layout_weight="1"
            android:textSize="14sp" />

        <ImageView
            android:layout_width="5dp"
            android:layout_height="40dp"
            app:srcCompat="@drawable/search"
            android:background="@drawable/button_search"
            android:id="@+id/search_btn_ti"
            android:layout_weight="1" />
    </LinearLayout>

    <ListView
        android:id="@+id/lsw_track_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >
    </ListView>

</LinearLayout>